# ============================================================
# localhost
# ============================================================

<VirtualHost *:80>

    ServerName localhost

    DocumentRoot "/var/www/sites/main"

    <Directory "/var/www/sites/main">
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    DirectoryIndex index.php index.html

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://php-main:9000"
    </FilesMatch>

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined

</VirtualHost>


# ============================================================
# app1.localhost
# ============================================================

<VirtualHost *:80>

    ServerName app1.localhost

    DocumentRoot "/var/www/sites/admin"

    <Directory "/var/www/sites/admin">
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    DirectoryIndex index.php index.html

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://php-admin:9000"
    </FilesMatch>

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined

</VirtualHost>